diff '--color=auto' -Nuar linux-6.12.3.orig/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm.c linux-6.12.3/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm.c
--- linux-6.12.3.orig/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm.c	2024-12-06 07:20:46.000000000 +0100
+++ linux-6.12.3/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm.c	2024-12-07 19:27:53.864401317 +0100
@@ -11310,6 +11310,13 @@
 			break;
 		}
 
+                /* If this plane has no width or height, it's not visibile,
+                 * consider mode change.
+                 * */
+                if (!(plane_state->src_h >> 16) || !(plane_state->src_w >> 16)) {
+                    consider_mode_change = true;
+                    break;
+                }
 		dm_get_plane_scale(plane_state, &new_scale_w, &new_scale_h);
 		dm_get_plane_scale(old_plane_state, &old_scale_w, &old_scale_h);
 		if (new_scale_w != old_scale_w || new_scale_h != old_scale_h) {
